#version 450


in  vec4  vPosition;
in  vec4  vColor;
out vec4  outColor;

uniform float time = 0.0;
uniform float color2 = 0.0;

layout(location = 4) uniform sampler2D textureMap;


void main ()
{ 

	//outColor = vPosition;

	//vec4 cPos = vPosition;


	outColor  = texture(textureMap, vPosition.xy);
	outColor.r *= color2;
	outColor.g *= color2;
	outColor.b *= color2;
	
	//outColor = texture(textureMap, vPosition.xy);
}